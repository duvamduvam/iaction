{
  "name": "@iaction/sidecar",
  "private": true,
  "version": "0.3.1",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc && node scripts/empreinte.mjs dist",
    "build:dev": "tsc --outDir dist-dev && node scripts/empreinte.mjs dist-dev",
    "build:verif": "tsc --outDir dist-verif && node scripts/empreinte.mjs dist-verif",
    "test": "npm run build:verif && node test/tous.mjs"
  },
  "devDependencies": {
    "@types/node": "^22",
    "typescript": "~5.8.3"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.214",
    "@huggingface/transformers": "^4.2.0",
    "kokoro-js": "^1.2.1",
    "yaml": "^2.9.0",
    "zod": "^4.0.0"
  }
}
