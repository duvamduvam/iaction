{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window and every window opened from it (T-062)",
  "windows": ["main", "main-*"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
